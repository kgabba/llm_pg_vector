# 🚀 RAG API + Auth (FastAPI + PostgreSQL + pgvector)

Backend-сервис для обработки документов и интеллектуального поиска с использованием RAG (Retrieval-Augmented Generation) архитектуры. Сервис предоставляет аутентификацию, управление ролями, загрузку документов и семантический поиск по текстовым данным.

## 📋 Содержание
- [Особенности](#особенности)
- [Стек технологий](#-стек-технологий)
- [Архитектура](#-архитектура-docker-compose)
- [Структура БД](#-структура-бд)
- [Установка и запуск](#-установка-и-запуск)
- [API Эндпоинты](#-api-эндпоинты)
- [Роли и доступ](#-роли-и-доступ)
- [Работа с документами](#-работа-с-документами)
- [RAG-пайплайн](#-rag-пайплайн)
- [Планы развития](#-планы-развития)
- [Разработка](#-разработка)

## ✨ Особенности

- 🔐 **Аутентификация и авторизация** через JWT токены в cookies
- 👥 **Система ролей** (`user`, `admin`, `moderator`) с разграничением прав доступа
- 📄 **Загрузка документов** поддержка PDF, DOCX форматов
- 🧠 **RAG-система** с семантическим поиском на основе векторных эмбеддингов
- 🗄️ **Векторная БД** PostgreSQL с расширением pgvector
- 🐳 **Полная контейнеризация** через Docker Compose
- 📊 **Администрирование** веб-интерфейс pgAdmin для управления БД

## 🧱 Стек технологий

- **Backend Framework**: FastAPI, Uvicorn
- **База данных**: PostgreSQL 15 + pgvector
- **Язык программирования**: Python 3.11
- **AI/ML**: 
  - OpenAI API (gpt-4o-mini, text-embedding-3-small)
  - LangChain для обработки документов
- **Обработка документов**: PyPDF2, python-docx
- **Аутентификация**: JWT (JSON Web Tokens)
- **Контейнеризация**: Docker, Docker Compose
- **Администрирование БД**: pgAdmin 4

## 📦 Архитектура (docker-compose)

Проект состоит из трех основных сервисов:

| Сервис | Описание | Порт | Доступ |
|--------|----------|------|--------|
| `api_app` | FastAPI бэкенд приложение | 8000 | http://localhost:8000 |
| `postgres_db` | PostgreSQL с расширением pgvector | 5432 | localhost:5432 |
| `pg_admin` | Веб-интерфейс для администрирования БД | 5050 | http://localhost:5050 |

## 🗄️ Структура БД

### Таблица пользователей
```sql
CREATE TABLE IF NOT EXISTS users (
    id SERIAL PRIMARY KEY,
    username TEXT NOT NULL UNIQUE,
    roles TEXT[] DEFAULT ARRAY['user']::TEXT[],
    hash_psw TEXT NOT NULL,
    session_token TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Таблица векторных эмбеддингов
```sql
CREATE EXTENSION IF NOT EXISTS vector;

CREATE TABLE IF NOT EXISTS embeddings (
    id SERIAL PRIMARY KEY,
    text TEXT NOT NULL,
    embedding VECTOR(1536),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    metadata JSONB DEFAULT '{}'::jsonb
);

CREATE INDEX IF NOT EXISTS embedding_index ON embeddings USING ivfflat (embedding vector_cosine_ops);
```

## 🚀 Установка и запуск

### 1. Предварительные требования
- Установленный Docker и Docker Compose
- Ключ OpenAI API

### 2. Настройка окружения
Создайте файл `.env` в корневой директории проекта:

```env
# Настройки базы данных
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=mydb
DB_HOST=postgres_db
DB_PORT=5432

# Настройки OpenAI
OPENAI_API_KEY=sk-your-openai-api-key-here

# Настройки JWT
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# Настройки приложения
EMBEDDING_MODEL=text-embedding-3-small
LLM_MODEL=gpt-4o-mini
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
TOP_K_RESULTS=3
```

### 3. Запуск приложения
```bash
# Сборка и запуск контейнеров
docker-compose up --build

# Для запуска в фоновом режиме
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f api_app

# Остановка контейнеров
docker-compose down
```

### 4. Проверка работоспособности
После запуска проверьте доступность сервисов:

- **FastAPI API & документация**: http://localhost:8000/docs
- **pgAdmin (администрирование БД)**: http://localhost:5050
  - Логин: `admin@admin.com`
  - Пароль: `admin`
- **PostgreSQL**: `localhost:5432`

## 🔐 API Эндпоинты

### Аутентификация

#### Регистрация пользователя
```http
POST /bd/reg_user
Content-Type: application/json

{
  "username": "testuser",
  "password": "securepassword123",
  "password_repeat": "securepassword123"
}
```

#### Вход в систему
```http
POST /auth/login
Content-Type: application/json

{
  "username": "testuser",
  "password": "securepassword123"
}
```
После успешного входа JWT токен устанавливается в cookie `jwt_personal_session_token`.

### Управление пользователями (требуется роль moderator)

#### Обновление ролей пользователя
```http
POST /bd/update_roles
Content-Type: multipart/form-data

username=testuser
action=add  # или "remove"
```

#### Получение списка пользователей
```http
GET /bd/get_users
```

### Работа с документами (требуется роль admin)

#### Загрузка текста для создания эмбеддингов
```http
POST /llm/embed
Content-Type: application/json

{
  "text": "Длинный текст для обработки и создания векторных представлений..."
}
```

#### Загрузка файла (PDF/DOCX)
```http
POST /llm/embed_file
Content-Type: multipart/form-data

file: <выберите файл>
```

Поддерживаемые форматы:
- **PDF**: извлечение текста со всех страниц
- **DOCX**: извлечение текста из параграфов и таблиц

### RAG-запросы (требуется роль user)

#### Семантический поиск и генерация ответа
```http
POST /llm/ask
Content-Type: application/json

{
  "text": "Какой вопрос у вас есть к загруженным документам?",
  "top_k": 3  # опционально, количество релевантных чанков
}
```

Пример ответа:
```json
{
  "answer": "Ответ, сгенерированный на основе найденной информации...",
  "context_used": [
    "Релевантный текст из первого чанка...",
    "Релевантный текст из второго чанка..."
  ],
  "sources": 2
}
```

## 👥 Роли и доступ

Система использует три основные роли с различными уровнями доступа:

| Роль | Описание | Доступные эндпоинты |
|------|----------|---------------------|
| **user** | Обычный пользователь | `/llm/ask` |
| **admin** | Администратор контента | Все эндпоинты `user` + `/llm/embed`, `/llm/embed_file` |
| **moderator** | Модератор пользователей | Все эндпоинты `user` + `/bd/update_roles`, `/bd/get_users` |

**Примечание**: Один пользователь может иметь несколько ролей.

## 📄 Работа с документами

### Процесс обработки документов
1. **Загрузка файла** → API принимает PDF или DOCX
2. **Извлечение текста** → использование PyPDF2 / python-docx
3. **Разделение на чанки** → текст делится на фрагменты фиксированного размера с перекрытием
4. **Создание эмбеддингов** → генерация векторных представлений через OpenAI API
5. **Сохранение в БД** → текст и векторные эмбеддинги сохраняются в PostgreSQL

### Настройки обработки текста
```python
CHUNK_SIZE = 1000  # символов на чанк
CHUNK_OVERLAP = 200  # перекрытие между чанками
EMBEDDING_DIMENSIONS = 1536  # размерность эмбеддингов text-embedding-3-small
```

## 🧠 RAG-пайплайн

### Процесс обработки запроса
1. **Получение вопроса** → пользователь отправляет текстовый запрос
2. **Создание эмбеддинга вопроса** → генерация векторного представления запроса
3. **Семантический поиск** → поиск `top_k` наиболее релевантных чанков через pgvector
4. **Проверка релевантности** → фильтрация результатов по порогу схожести
5. **Формирование промпта** → создание контекста для LLM
6. **Генерация ответа** → использование gpt-4o-mini для создания финального ответа
7. **Возврат результата** → ответ с указанием использованных источников

### Промпт-шаблон
```
Используй только следующую информацию для ответа на вопрос:

[Контекст из релевантных чанков]

Вопрос: {question}

Если в предоставленной информации нет ответа на вопрос, скажи: "Нет такой информации в базе".

Ответ:
```

## 📊 Администрирование

### Доступ к pgAdmin
1. Перейдите на http://localhost:5050
2. Войдите с учетными данными:
   - Email: `admin@admin.com`
   - Пароль: `admin`
3. Добавьте новый сервер:
   - Name: `RAG Database`
   - Host: `postgres_db`
   - Port: `5432`
   - Username: `postgres`
   - Password: `postgres`

### Полезные SQL-запросы

```sql
-- Просмотр всех пользователей
SELECT id, username, roles, created_at FROM users;

-- Просмотр количества эмбеддингов
SELECT COUNT(*) FROM embeddings;

-- Поиск похожих эмбеддингов
SELECT text, embedding <=> $1 AS similarity 
FROM embeddings 
ORDER BY similarity 
LIMIT 5;
```

## 🔧 Планы развития

### Ближайшие улучшения
- [ ] **Telegram-бот** для доступа к API через мессенджер
- [ ] **Метаданные для файлов** (название, дата загрузки, тип)
- [ ] **Асинхронный PostgreSQL драйвер** (asyncpg) для улучшения производительности
- [ ] **SQLAlchemy ORM** для более удобной работы с БД

### Будущие возможности
- [ ] **Веб-админка** для управления пользователями и документами
- [ ] **Поддержка дополнительных форматов** (TXT, PPTX, HTML)
- [ ] **Гибкая настройка чанкинга** (разные стратегии разделения)
- [ ] **Кэширование эмбеддингов** для повторяющихся запросов
- [ ] **Аналитика использования** (статистика запросов)
- [ ] **Поддержка нескольких моделей эмбеддингов**
- [ ] **Экспорт/импорт данных** из векторной БД

## 🛠 Разработка

### Локальная разработка без Docker
```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск PostgreSQL с pgvector
# (требуется установленный PostgreSQL с расширением pgvector)

# Запуск приложения
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### Структура проекта
```
rag-api-auth/
├── src/
│   ├── main.py              # Основное приложение FastAPI
│   ├── auth.py             # Аутентификация и авторизация
│   ├── database.py         # Подключение к БД
│   ├── embeddings.py       # Работа с векторными эмбеддингами
│   ├── llm_processor.py    # Обработка LLM запросов
│   └── models.py          # Pydantic модели
├── Dockerfile             # Конфигурация Docker для API
├── docker-compose.yml     # Конфигурация всех сервисов
├── requirements.txt       # Зависимости Python
├── .env                  # Переменные окружения
└── README.md            # Документация
```

### Тестирование API
```bash
# Регистрация пользователя
curl -X POST "http://localhost:8000/bd/reg_user" \
  -H "Content-Type: application/json" \
  -d '{"username": "test", "password": "123", "password_repeat": "123"}'

# Вход в систему
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username": "test", "password": "123"}' \
  -c cookies.txt

# Загрузка файла (с использованием сохраненных cookies)
curl -X POST "http://localhost:8000/llm/embed_file" \
  -H "Cookie: jwt_personal_session_token=<ваш_токен>" \
  -F "file=@document.pdf"

# RAG запрос
curl -X POST "http://localhost:8000/llm/ask" \
  -H "Content-Type: application/json" \
  -H "Cookie: jwt_personal_session_token=<ваш_токен>" \
  -d '{"text": "Ваш вопрос здесь"}'
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add some amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности см. в файле LICENSE.

## ⚠️ Важные замечания

1. **Безопасность в production**:
   - Измените JWT_SECRET_KEY в production-окружении
   - Используйте надежные пароли для БД
   - Настройте HTTPS для защиты передаваемых данных

2. **Производительность**:
   - Настройте индексы pgvector для больших объемов данных
   - Рассмотрите использование более мощных инстансов для production

3. **OpenAI API**:
   - Мониторьте использование токенов для контроля расходов
   - Рассмотрите локальные модели эмбеддингов для экономии

4. **Масштабирование**:
   - Для больших нагрузок рассмотрите разделение сервисов
   - Добавьте балансировщик нагрузки при необходимости

---

**Примечание**: Этот проект предназначен для образовательных целей и демонстрации возможностей RAG-архитектуры. Для production использования требуется дополнительная настройка безопасности и оптимизация.
